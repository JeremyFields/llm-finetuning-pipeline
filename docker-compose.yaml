version: '3.8'
services:
  mlflow:
    image: ghcr.io/mlflow/mlflow:latest
    container_name: local_mlflow
    ports:
      - "5000:5000"
    volumes:
      - ./mlruns:/mlflow/mlruns
    command: >
      mlflow server
      --host 0.0.0.0
      --port 5000